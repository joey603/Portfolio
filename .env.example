# No API key is required for the current AI matching provider.
# Pollinations AI is used by the /api/job-match serverless route on Vercel.
# The site must be deployed on Vercel (or another host with serverless functions)
# so that /api/job-match exists in production.
